export let gameSettings: GameSettings = {
    gameWidth: window.innerWidth * window.devicePixelRatio,
    gameHeight: 1080,
    bgColor: "#ffffff",
    gravity: {x: 0, y: 200},
    debug: true,
    zoom: 1
}